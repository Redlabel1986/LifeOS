{
  "installCommand": "pnpm install --frozen-lockfile --prod=false",
  "buildCommand": "cd apps/web && pnpm run build && cp -r .vercel/output ../../.vercel/output",
  "outputDirectory": ".vercel/output",
  "framework": null
}
