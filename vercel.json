{
  "installCommand": "pnpm install --frozen-lockfile --prod=false",
  "buildCommand": "pnpm run build && cp -r .vercel/output ../../.vercel/output && node ../../scripts/patch-vercel.cjs",
  "outputDirectory": ".vercel/output",
  "framework": null
}
